version: '3.8'

services:
  youtube-proxy:
    image: starleydev/youtube-proxy:latest
    container_name: youtube-proxy
    ports:
      - '3000:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
